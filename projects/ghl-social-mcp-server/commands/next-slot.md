Find the next available posting slot.

Use get_next_slot to calculate when the next post can be scheduled (24h after the last scheduled post).

If $ARGUMENTS contains a location name, pass it as the location parameter.

Display the result as a human-readable date and time.
